# Deny all access to this directory
# Only PHP can access these files server-side
<FilesMatch ".*">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# For Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
